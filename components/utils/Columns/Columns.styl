.container
	display grid
	margin 0 auto
	overflow hidden
	width 100%
	column-gap 5rem
	row-gap 5rem
	grid-template-columns repeat(auto-fit, minmax(15rem, auto))

.center_column
	justify-content center
